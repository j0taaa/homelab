# Auto-generated by setup-env.sh from .env.master
# Variables are sourced from INFRA_* in .env.master

# Required for Cloudflare Tunnel (when using cloudflare profile)
CF_TUNNEL_TOKEN=your-cloudflare-tunnel-token

# Optional (uncomment in .env.master to override defaults):
# TRAEFIK_VERSION=v2.11
# TRAEFIK_HTTP_PORT=80
# TRAEFIK_DASHBOARD_PORT=8080
# PORTAINER_PORT=9000
# LOCAL_BIND_IP=127.0.0.1
# DOCKER_SOCKET=/var/run/docker.sock
