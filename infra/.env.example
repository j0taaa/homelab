# Set to "cloudflare" to enable the Cloudflare Tunnel container.
# Leave blank to run without the tunnel.
#COMPOSE_PROFILES=cloudflare

# Cloudflare Tunnel token (required only when COMPOSE_PROFILES includes "cloudflare").
#CF_TUNNEL_TOKEN=your-token-here

# Base domain for all services (used in Traefik routing labels)
#BASE_DOMAIN=jaypussy.site

# Traefik version (use v2.11 for macOS Docker Desktop, v3.x has compatibility issues)
#TRAEFIK_VERSION=v2.11

# Traefik HTTP port
#TRAEFIK_HTTP_PORT=80

# Local IP to bind admin interfaces (Traefik dashboard, Portainer)
#LOCAL_BIND_IP=100.120.251.69

# Traefik dashboard port
#TRAEFIK_DASHBOARD_PORT=8080

# Portainer UI port
#PORTAINER_PORT=9000

# Docker socket path (for macOS Docker Desktop, use ~/.docker/run/docker.sock)
#DOCKER_SOCKET=/var/run/docker.sock
